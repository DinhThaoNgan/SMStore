@model CuaHangBanSach.Models.Supplier
@{
    ViewData["Title"] = "Chi tiết nhà cung cấp";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center mb-4">📄 Chi tiết nhà cung cấp</h2>

<div class="card w-50 mx-auto shadow-sm">
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p><strong>Email:</strong> @Model.Email</p>
        <p><strong>SĐT:</strong> @Model.Phone</p>
        <p><strong>Địa chỉ:</strong> @Model.Address</p>
    </div>
</div>

<div class="text-center mt-4">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary me-2">✏️ Sửa</a>
    <a asp-action="Index" class="btn btn-secondary">← Quay lại danh sách</a>
</div>