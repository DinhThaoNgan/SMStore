@model CuaHangBanSach.ViewModels.ProductWithVariantsViewModel
@using Microsoft.AspNetCore.Mvc.Rendering

@{
    ViewData["Title"] = "Chỉnh sửa sản phẩm";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">Chỉnh sửa sản phẩm</h3>
        </div>
        <div class="card-body">
            <form asp-area="Admin" asp-controller="Product" asp-action="Update" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="Product.Id" />

                <div class="mb-3">
                    <label asp-for="Product.Name" class="form-label fw-semibold">Tên sản phẩm</label>
                    <input asp-for="Product.Name" class="form-control" />
                </div>

                <div class="mb-3">
                    <label asp-for="Product.Price" class="form-label fw-semibold">Giá</label>
                    <input asp-for="Product.Price" class="form-control" />
                </div>

                <div class="mb-3">
                    <label asp-for="Product.Description" class="form-label fw-semibold">Mô tả</label>
                    <textarea asp-for="Product.Description" class="form-control" rows="10" style="height: 200px;"></textarea>
                </div>


                <div class="row mb-3">
                    <div class="col-md-6">
                        <label asp-for="Product.CategoryId" class="form-label fw-semibold">Thể loại</label>
                        <select asp-for="Product.CategoryId" asp-items="ViewBag.Categories" class="form-select"></select>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Product.BrandId" class="form-label fw-semibold">Nhà xuất bản</label>
                        <select asp-for="Product.BrandId" asp-items="ViewBag.Brands" class="form-select"></select>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-semibold">Ảnh chính hiện tại</label><br />
                    <img src="@(!string.IsNullOrEmpty(Model.Product.ImageUrl) ? Model.Product.ImageUrl : Url.Content("~/images/no-image.png"))"
                         class="img-thumbnail border" style="max-width: 200px;" id="previewImage" />
                </div>

                <div class="mb-3">
                    <label class="form-label fw-semibold">Đổi ảnh chính (tùy chọn)</label>
                    <input type="file" name="imageUrl" class="form-control" id="imageInput" />
                </div>

                <hr />
                <h5 class="text-primary">Biến thể sản phẩm</h5>
                <div id="variant-container">
                    @for (int i = 0; i < Model.Variants.Count; i++)
                    {
                        <div class="row border p-3 mb-3 align-items-center">
                            <input type="hidden" name="Variants[@i].Id" value="@Model.Variants[i].Id" />
                            <div class="col-md-3">
                                <input name="Variants[@i].Color" value="@Model.Variants[i].Color" class="form-control" placeholder="Phiên bản" />
                            </div>
                            <div class="col-md-2">
                                <input name="Variants[@i].Size" value="@Model.Variants[i].Size" class="form-control" placeholder="Phân loại" />
                            </div>
                            <div class="col-md-2">
                                <input name="Variants[@i].StockQuantity" value="@Model.Variants[i].StockQuantity" type="number" class="form-control" placeholder="Tồn kho" />
                            </div>
                            <div class="col-md-3">
                                @if (!string.IsNullOrEmpty(Model.Variants[i].ImageUrl))
                                {
                                    <img id="variant-preview-@i" src="@Model.Variants[i].ImageUrl" class="img-thumbnail mb-2" style="max-height: 80px;" />
                                }
                                else
                                {
                                    <img id="variant-preview-@i" src="~/images/no-image.png" class="img-thumbnail mb-2" style="max-height: 80px;" />
                                }

                                <input type="file" name="Variants[@i].VariantImage" class="form-control" onchange="previewVariantImage(this, @i)" />
                                <small class="text-muted">Giữ nguyên nếu không thay đổi</small>
                            </div>

                            <div class="col-md-2 text-end">
                                <button type="button" class="btn btn-danger btn-sm" onclick="removeVariant(this)">🗑️ Xóa</button>
                            </div>
                        </div>
                    }
                </div>
                <button type="button" class="btn btn-outline-primary mb-4" onclick="addVariant()">➕ Thêm biến thể</button>

                <div class="d-flex justify-content-between">
                    <a asp-area="Admin" asp-controller="Product" asp-action="Index" class="btn btn-outline-secondary">← Quay lại</a>
                    <button type="submit" class="btn btn-success px-4">💾 Lưu thay đổi</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('imageInput').addEventListener('change', function (event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    document.getElementById('previewImage').src = e.target.result;
                };
                reader.readAsDataURL(file);
            }
        });

        let index = @Model.Variants.Count;

                function addVariant() {
            const container = document.getElementById('variant-container');
            const html = `
                <div class="row border p-3 mb-3 align-items-center">
                    <input type="hidden" name="Variants[${index}].Id" value="0" />
                    <div class="col-md-3">
                        <input name="Variants[${index}].Color" class="form-control" placeholder="Phiên bản" />
                    </div>
                    <div class="col-md-2">
                        <input name="Variants[${index}].Size" class="form-control" placeholder="Phân loại" />
                    </div>
                    <div class="col-md-2">
                        <input name="Variants[${index}].StockQuantity" type="number" class="form-control" placeholder="Tồn kho" />
                    </div>
                    <div class="col-md-3">
                        <img id="variant-preview-${index}" src="/images/no-image.png" class="img-thumbnail mb-2" style="max-height: 80px;" />
                        <input type="file" name="Variants[${index}].VariantImage" class="form-control"
                               onchange="previewVariantImage(this, ${index})" />
                    </div>
                    <div class="col-md-2 text-end">
                        <button type="button" class="btn btn-danger btn-sm" onclick="removeVariant(this)">🗑️ Xóa</button>
                    </div>
                </div>`;
            container.insertAdjacentHTML('beforeend', html);
            index++;
        }

                function previewVariantImage(input, index) {
            const file = input.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    const preview = document.getElementById(`variant-preview-${index}`);
                    if (preview) {
                        preview.src = e.target.result;
                    }
                };
                reader.readAsDataURL(file);
            }
        }

        function removeVariant(button) {
            const row = button.closest('.row');
            row.remove();
        }
    </script>
    @await Html.PartialAsync("_ValidationScriptsPartial")
}