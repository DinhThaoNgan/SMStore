@model int

<form class="d-flex me-3" asp-controller="ShoppingCart" asp-action="Index" method="get">
    <button class="btn btn-outline-dark" type="submit">
        <i class="bi-cart-fill me-1"></i> Giỏ hàng
        @if (Model > 0)
        {
            <span class="badge bg-danger text-white ms-1">@Model</span>
        }
    </button>
</form>
